# ── API Gateway ────────────────────────────────────────────────────
VITE_API_GATEWAY_URL=http://localhost:8080

# ── OAuth2 / OIDC providers ─────────────────────────────────────────
# All providers use PKCE flow (no client secret in browser)

# Google
VITE_OIDC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_OIDC_GOOGLE_AUTHORITY=https://accounts.google.com

# GitHub (via GitHub OAuth App + PKCE proxy — or Keycloak federation)
VITE_OIDC_GITHUB_CLIENT_ID=your-github-oauth-app-client-id
VITE_OIDC_GITHUB_AUTHORITY=https://github.com

# App URL (used as redirect URI — must be registered in each OAuth app)
VITE_APP_URL=http://localhost:5173

# Optional: Keycloak / custom IdP (takes precedence if set)
# VITE_OIDC_AUTHORITY=http://localhost:8180/realms/workforcehub
# VITE_OIDC_CLIENT_ID=workforcehub-frontend
